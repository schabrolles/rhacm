apiVersion: velero.io/v1
kind: Restore
metadata:
  namespace: openshift-adp
  name: restore
spec:
  scheduleName: lets chat-replication
  # includedResources:
  #   - pv
  includedNamespaces:
    - letschat-replication
